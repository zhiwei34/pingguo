// Una funcion es un conjunto de instrucciones que se agrupan para realizar una tarea específica y que se pueden reutilizar facilmente.

function sumar(a, b) {
    return a + b;
}

const resultado = sumar(10, 5);

console.log(resultado); // 15